// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class CliEnvValues {
  const CliEnvValues({required this.ftnlFlagsConfig, required this.json, required this.localappdata, required this.stateDir, required this.timeout, required this.tunnel, required this.url, required this.xdgStateHome, required this.shell, required this.accept, required this.applicationId, required this.expiresIn, required this.maxFileBytes, required this.maxFiles, required this.fileId, required this.force, required this.out, required this.file, required this.jobId, required this.mediaType});

  final String? ftnlFlagsConfig;
  final bool json;
  final String? localappdata;
  final String? stateDir;
  final int timeout;
  final String? tunnel;
  final String url;
  final String? xdgStateHome;
  final String shell;
  final String accept;
  final String? applicationId;
  final int expiresIn;
  final int maxFileBytes;
  final int maxFiles;
  final String? fileId;
  final bool force;
  final String? out;
  final String? file;
  final String? jobId;
  final String mediaType;
}

/// Pure: resolve values from an explicit lookup.
CliEnvValues loadFrom(String? Function(String key) lookup) {
  return CliEnvValues(
    ftnlFlagsConfig: _nonEmpty(lookup('FTNL_FLAGS_CONFIG')),
    json: _parseBool(lookup('FTNL_JSON'), false),
    localappdata: _nonEmpty(lookup('LOCALAPPDATA')),
    stateDir: _nonEmpty(lookup('FTNL_STATE_DIR')),
    timeout: _parseInt(lookup('FTNL_TIMEOUT_MS'), 30000),
    tunnel: _nonEmpty(lookup('FTNL_TUNNEL_ID')),
    url: _nonEmpty(lookup('FTNL_BASE_URL')) ?? 'https://api.file-tunnel.dev',
    xdgStateHome: _nonEmpty(lookup('XDG_STATE_HOME')),
    shell: _nonEmpty(lookup('FTNL_COMPLETION_SHELL')) ?? 'bash',
    accept: _nonEmpty(lookup('FTNL_ACCEPT')) ?? '["image/*"]',
    applicationId: _nonEmpty(lookup('FTNL_APPLICATION_ID')),
    expiresIn: _parseInt(lookup('FTNL_EXPIRES_IN_SECONDS'), 600),
    maxFileBytes: _parseInt(lookup('FTNL_MAX_FILE_BYTES'), 52428800),
    maxFiles: _parseInt(lookup('FTNL_MAX_FILES'), 10),
    fileId: _nonEmpty(lookup('FTNL_FILE_ID')),
    force: _parseBool(lookup('FTNL_FORCE'), false),
    out: _nonEmpty(lookup('FTNL_OUT')),
    file: _nonEmpty(lookup('FTNL_FILE')),
    jobId: _nonEmpty(lookup('FTNL_JOB_ID')),
    mediaType: _nonEmpty(lookup('FTNL_MEDIA_TYPE')) ?? 'application/octet-stream',
  );
}

/// Resolve values from a map of env keys.
CliEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
CliEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final ftnlFlagsConfig = pick(['FTNL_FLAGS_CONFIG'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (ftnlFlagsConfig != null) out['FTNL_FLAGS_CONFIG'] = ftnlFlagsConfig;
  final json = pick(['FTNL_JSON'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (json != null) out['FTNL_JSON'] = json;
  final localappdata = pick(['LOCALAPPDATA'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (localappdata != null) out['LOCALAPPDATA'] = localappdata;
  final stateDir = pick(['FTNL_STATE_DIR'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (stateDir != null) out['FTNL_STATE_DIR'] = stateDir;
  final timeout = pick(['FTNL_TIMEOUT_MS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '30000');
  if (timeout != null) out['FTNL_TIMEOUT_MS'] = timeout;
  final tunnel = pick(['FTNL_TUNNEL_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (tunnel != null) out['FTNL_TUNNEL_ID'] = tunnel;
  final url = pick(['FTNL_BASE_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'https://api.file-tunnel.dev');
  if (url != null) out['FTNL_BASE_URL'] = url;
  final xdgStateHome = pick(['XDG_STATE_HOME'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (xdgStateHome != null) out['XDG_STATE_HOME'] = xdgStateHome;
  final shell = pick(['FTNL_COMPLETION_SHELL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'bash');
  if (shell != null) out['FTNL_COMPLETION_SHELL'] = shell;
  final accept = pick(['FTNL_ACCEPT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '["image/*"]');
  if (accept != null) out['FTNL_ACCEPT'] = accept;
  final applicationId = pick(['FTNL_APPLICATION_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (applicationId != null) out['FTNL_APPLICATION_ID'] = applicationId;
  final expiresIn = pick(['FTNL_EXPIRES_IN_SECONDS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '600');
  if (expiresIn != null) out['FTNL_EXPIRES_IN_SECONDS'] = expiresIn;
  final maxFileBytes = pick(['FTNL_MAX_FILE_BYTES'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '52428800');
  if (maxFileBytes != null) out['FTNL_MAX_FILE_BYTES'] = maxFileBytes;
  final maxFiles = pick(['FTNL_MAX_FILES'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '10');
  if (maxFiles != null) out['FTNL_MAX_FILES'] = maxFiles;
  final fileId = pick(['FTNL_FILE_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fileId != null) out['FTNL_FILE_ID'] = fileId;
  final force = pick(['FTNL_FORCE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (force != null) out['FTNL_FORCE'] = force;
  final out = pick(['FTNL_OUT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (out != null) out['FTNL_OUT'] = out;
  final file = pick(['FTNL_FILE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (file != null) out['FTNL_FILE'] = file;
  final jobId = pick(['FTNL_JOB_ID'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (jobId != null) out['FTNL_JOB_ID'] = jobId;
  final mediaType = pick(['FTNL_MEDIA_TYPE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'application/octet-stream');
  if (mediaType != null) out['FTNL_MEDIA_TYPE'] = mediaType;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
